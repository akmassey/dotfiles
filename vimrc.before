" Splitting my vimrc so it's compatible with Janus

" Map the leader to something more reasonable
let mapleader=","

" Ensure powerline settings are loaded properly.  More info: 
"     https://github.com/Lokaltog/vim-powerline
let g:Powerline_symbols = 'fancy'
set encoding=utf-8

call janus#disable_plugin('vimwiki', "It's causing errors when using Markdown.")
